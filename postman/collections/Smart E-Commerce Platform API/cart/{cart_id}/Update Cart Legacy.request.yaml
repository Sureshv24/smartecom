$kind: http-request
url: "{{baseUrl}}/cart/:cart_id"
method: PUT
headers:
  Content-Type: application/json
  Accept: application/json
pathVariables:
  cart_id: "8883"
body:
  type: json
  content: |-
    {
      "quantity": 5571
    }
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
settings: {}
examples: ./.resources/Update Cart Legacy.resources/examples
order: 1000
