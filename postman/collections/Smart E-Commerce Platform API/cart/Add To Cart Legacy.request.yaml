$kind: http-request
url: "{{baseUrl}}/cart"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
body:
  type: json
  content: |-
    {
      "product_id": 3345,
      "quantity": 1
    }
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
settings: {}
examples: ./.resources/Add To Cart Legacy.resources/examples
order: 6000
