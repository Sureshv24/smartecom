$kind: http-request
url: "{{baseUrl}}/orders"
method: GET
headers:
  Accept: application/json
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
settings: {}
examples: ./.resources/Get My Orders.resources/examples
order: 2000
