$kind: http-request
url: "{{baseUrl}}/orders"
method: POST
headers:
  Accept: application/json
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
settings: {}
examples: ./.resources/Create Order.resources/examples
order: 3000
