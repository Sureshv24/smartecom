$kind: http-request
url: "{{baseUrl}}/checkout"
method: POST
headers:
  Accept: application/json
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
settings: {}
examples: ./.resources/Create Checkout.resources/examples
order: 2000
