$kind: http-request
url: "{{baseUrl}}/auth/customer-test"
method: GET
headers:
  Accept: application/json
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
settings: {}
examples: ./.resources/Customer Test.resources/examples
order: 1000
