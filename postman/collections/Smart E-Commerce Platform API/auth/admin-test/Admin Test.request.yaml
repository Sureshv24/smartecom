$kind: http-request
url: "{{baseUrl}}/auth/admin-test"
method: GET
headers:
  Accept: application/json
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
settings: {}
examples: ./.resources/Admin Test.resources/examples
order: 1000
