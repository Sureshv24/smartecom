$kind: http-request
url: "{{baseUrl}}/auth/me"
method: GET
headers:
  Accept: application/json
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
settings: {}
examples: ./.resources/Get Me.resources/examples
order: 1000
